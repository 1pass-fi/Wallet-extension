import { BackgroundAccountManager, PopupAccountManager } from './AccountManager'

export const backgroundAccount = new BackgroundAccountManager()
export const popupAccount = new PopupAccountManager()
